import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#039be5',
    },
    secondary: {
      main: '#ffab00',
    },
    text: {
      primary: '#252525',
      secondary: '#f5f5f5',
    },
  }
});

export default theme;