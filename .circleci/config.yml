version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:14.15.1
    steps:
      - checkout
      - run: echo "yarn installing"
      - run: yarn install
      - run: CI=true yarn run build

workflows:
  install-and-run:
    jobs:
      - build
